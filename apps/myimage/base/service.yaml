apiVersion: v1
kind: Service
metadata:
  name: myimage
  labels:
    app.kubernetes.io/name: myimage
    app.kubernetes.io/part-of: argocd-learning
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: myimage
  ports:
    - name: http
      port: 80
      targetPort: http
